services:
  - type: web
    name: language-teachlox
    env: docker
    region: oregon
    disk:
      name: sqlite-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: DATABASE_URL
        value: /var/data/language_teacher.db
      - key: GEMINI_API_KEY
        sync: false
